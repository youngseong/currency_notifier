[project]
name = "currency-notifier"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "python-telegram-bot",
    "dotenv>=0.9.9",
    "ruff>=0.15.8",
]

[tool.uv]
package = false
